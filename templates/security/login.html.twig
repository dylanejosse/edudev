<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Edudev - Login</title>
        <link rel="icon" href="images/logo-edudev.png">
        <meta name="description" content="Connectez vous dès maintenant sur notre plateforme edudev afin de retrouver les projets proposés par les membres de notre communauté et évoluer sur des projets en collaboration avec d'autres étudiants du secteur des nouvelles technologies.">
        
        
        {# Bootstrap link #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
        
        {# My style.css Sheet's link #}
        <link rel="stylesheet" href="css/style.css">
        
        {# Google Font's Link #}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Questrial&display=swap" rel="stylesheet">
        
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}{% endblock %}

        {% block javascripts %}{% endblock %}
    </head>

<section>

    <div class="d-flex align-items-center">

            <div class="d-flex connexion align-items-center col-12 col-xl-6 col-xxl-5">

            <div class="d-flex flex-wrap align-items-center justify-content-center col-11 connexion-block container bg-light">

                <form method="post" class="col-12 p-1 p-md-5">

                    <div class="my-3">
                        <a href="{{ path('app_home') }}">Accéder à l'accueil Edudev</a>
                    </div>

                    {% if app.user %}
                        <div class="mb-3">
                            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                        </div>
                    {% endif %}

                    <h1 class="h3 mb-3 fw-bold">Connexion</h1>

                    {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}

                    <label for="inputEmail" class="mb-2">Adresse mail</label>
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control mb-3" autocomplete="email" required autofocus>
                    <label for="inputPassword" class="mb-2">Mot de passe</label>
                    <input type="password" name="password" id="inputPassword" class="form-control mb-3" autocomplete="current-password" required>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                    {#
                        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                        See https://symfony.com/doc/current/security/remember_me.html

                        <div class="checkbox mb-3">
                            <label>
                                <input type="checkbox" name="_remember_me"> Remember me
                            </label>
                        </div>
                    #}

                    <button class="btn bg-green" type="submit">
                        Se connecter
                    </button>

                    <a href="{{ path('app_register') }}" class="btn btn-secondary my-2 mx-md-3">Créer un compte</a>

                </form>

            </div>

            </div>

            <div class="connexion-img col-xl-6 col-xxl-7 d-none d-xl-block">
            
            </div>

    </div>

</section>

</html>
