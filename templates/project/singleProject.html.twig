{% extends 'base.html.twig' %}

{% block title %}{{ currentProject.name }}{% endblock %}

{% block stylesheets %}
    {# My style.css Sheet's link #}
    <link rel="stylesheet" href="../css/style.css">
{% endblock %}

{% block body %}

<section class="singleProject">

    <div class="container px-4 pt-4 px-lg-0">

        <div class="row">

            <div class="d-flex flex-wrap col-12 p-0 mb-3 mb-lg-5">
            
                <div class="d-flex justify-content-center align-items-center col-12 col-lg-4 mb-3 m-lg-0">

                    <div>
                
                        <img class="single-project-image col-6 col-lg-10" src="../{{ currentProject.image }}" alt="">

                    </div>
                
                </div>

                <div class="col-12 col-lg-8 m-0">

                    <div class="d-flex col-12 mb-3">
                        <div class="col-12 col-lg-7">
                            <h1 class="text-center text-lg-start px-3 m-0">{{ currentProject.name }}</h1>
                        </div>

                        <div class="d-flex align-items-center justify-content-end col-12 col-lg-5">
                            <div>
                                <a class="btn btn-primary" href="{{ path('app_project_list') }}">Trouver un autre projet</a>
                            </div>
                            {% if currentUser == currentProject.user %}
                            <div>
                                <a class="btn btn-success ms-2" href="{{ path('app_current_user_projects') }}">Mes projets</a>
                            </div>
                            {% endif %}
                        </div>
                    </div>

                    <div class="rounded p-3">

                        <p class="text-break">{{ currentProject.summary }}</p>

                        <p>Pour contacter le chef de projet, veuillez le contacter par mail à l'adresse mail suivante : {{ projectOwner.email }}</p>

                    </div>
                
                </div>

            </div>

            <div class="d-flex flex-wrap mb-3 p-0">

                <div class="col-12 col-lg-4 d-flex justify-content-center mb-3 m-lg-0">

                    <div class="h-auto col-12 col-lg-9 rounded border p-3">
                    
                        <h3 class="fst-italic mb-3">Informations générales</h3>

                        <p><span class="fst-italic text-decoration-underline">Date de mise en ligne</span> : {{ currentProject.createdAt | date("d/m/y") }}</p>

                        <p><span class="fst-italic text-decoration-underline">Satut du projet</span> : {{ currentProject.status }}</p>

                        <p><span class="fst-italic text-decoration-underline">Localisation </span>: France (remote)</p>

                        {% if currentUser == currentProject.user %}
                        <div class="d-lg-flex flex-wrap d-none align-items-center col-12">
                            <a class="btn btn-warning col-8 mb-3" href="{{ path('app_project_edit', {id: currentProject.id}) }}">Modifier</a>
                            <a class="btn btn-danger col-8" href="{{ path('app_project_delete', {id: currentProject.id}) }}">Supprimer</a>
                        </div>
                        {% else %}
                        <div class="d-lg-flex d-none align-items-center col-12">
                            <a class="btn btn-success col-7" href="">Candidater</a>
                        </div>
                        {% endif %}
                    
                    </div>

                </div>

                <div class="col-12 col-lg-8 rounded border p-3">
                
                    <h3 class="fst-italic mb-3">Gros plan sur le projet</h3>

                    <p class="text-break"><span class="fst-italic text-decoration-underline">Description </span>: {{ currentProject.description }}</p>

                    <p>Pour participer à ce projet, il est recommandé par le project owner d'un certain nombre d'heures par semaine. Ici, {{ currentProject.timeNecessaryWeek }} serait idéal !</p>
                
                </div>

            </div>

            <div class="d-flex justify-content-center justify-content-lg-end p-0">
            
                <div class="col-12 col-lg-8 rounded border p-3">
                
                    <div class="">
                    
                        <h2 class="fst-italic">Informations techniques</h2>

                        <p><span class="fst-italic text-decoration-underline">Niveau d'études demandé </span>: {{ currentProject.studyLevel }}</p>

                        <p><span class="fst-italic text-decoration-underline">Stack technique </span>: {% for technologie in currentProject.technologies %}

                             {{technologie.name}}

                             {% endfor %}
                        </p>

                        <p class="text-break"><span class="fst-italic text-decoration-underline">Besoin du projet </span>: {{ currentProject.needDescription }}</p>
                    
                    </div>
                
                </div>
            
            </div>

            <div class="d-lg-none d-flex p-0">
            
                <div class="d-flex justify-content-center col-12 pt-3">

                        {% if currentUser == currentProject.user %}
                        <div class="d-flex flex-wrap align-items-center justify-content-center col-12">
                            <a class="col-7 btn btn-primary my-3" href="{{ path('app_current_user_projects')}}">Voir mes projets</a>
                            <a class="col-7 btn btn-warning my-3" href="{{ path('app_project_edit', {id: currentProject.id}) }}">Modifier</a>
                            <a class="col-7 btn btn-danger my-3" href="{{ path('app_project_delete', {id: currentProject.id}) }}">Supprimer</a>
                        </div>
                        {% else %}
                        <div class="d-flex align-items-center justify-content-center col-7">
                            <a class="btn btn-success" href="">Candidater</a>
                        </div>
                        {% endif %}
                
                </div>

            </div>

        </div>

    </div>

</section>
{% endblock %}